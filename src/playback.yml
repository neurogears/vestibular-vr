%YAML 1.1
---
# yaml-language-server: $schema=schema.json

metadata:
  animalId: plimbo
  rootPath: D:/TrialLogicTestData/
  runGain: 1

motorSettings:
  nominalPulseInterval: 200
  initialPulseInterval: 200
  pulseStepInterval: 2
  pulsePeriod: 10

blocks: [
  &LinearPlaybackMismatch
  {
    alias: LinearPlaybackMismatch,
    haltProtocol: {
      minumumDelay: 1,
      randomDelay: 0,
      haltTime: 1,
      haltGain: 1,
    },
    stimulus: {
      type: grating,
      spatialFrequency: 28.8
    },
    haltProbability: 1,
    totalRuntime: 900,
    playbackToVisualGain: 0.0008,
    playbackToRunGain: 0.0008,
    runThreshold: 10,
    decayTimestep: 0.1,
    runThresholdDecay: -0.1, 
    playbackFilePath: "Z:/data/ONIX/20240730_Mismatch_Experiment/MMday2_playback/playback_file_B3M2_MM2.csv"
  }
]